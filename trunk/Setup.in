# Setup file to build the Sybase module

*shared*

# Define the root directory of your Sybase installation.  eg. on my
# Linux box, which isql yields /opt/sybase/bin/isql, so SYBROOT is
# /opt/sybase

SYBROOT = /opt/sybase

SYBINC = $(SYBROOT)/include
DATETIMEINC = DateTime/mxDateTime

# Uncomment exactly one of the following:

# For Linux
SYBLIB = $(SYBROOT)/lib -lblk -lct -lcs -lsybtcl -lcomn -linsck -lintl
# For Solaris
#SYBLIB = $(SYBROOT)/lib -lblk -lct -lcs -ltcl -lcomn -lintl -lxnet

sybasect sybasect.c ctx.c con.c blk.c cmd.c datafmt.c buffer.c numeric.c msgs.c -I$(DATETIMEINC) -I$(SYBINC) -L$(SYBLIB)

