- Complete CS_CONTEXT_cs_config

- Enforce single result set over each connection by attaching all
  cursors to parent connection with a weakref.

- Change documentation to describe Sybase CT library wrapping not CT
  function semantics.

- Change exception handling in Sybase.py

  * use message codes in ct_diag/cs_diag to determine type of
    exception to raise

  * see if callbacks might not be a better way to go - try in mssql
    module first

- Connection.rollback() cannot be called while a cursor is in flight...

- Cursor.callproc() does not return parameters.

- Type Objects and Constructors

      Date(year,month,day)
      Time(hour,minute,second)
      Timestamp(year,month,day,hour,minute,second)
      DateFromTicks(ticks)
      TimeFromTicks(ticks)
      TimestampFromTicks(ticks)
      Binary(string)
      STRING
      BINARY
      NUMBER
      DATETIME
      ROWID

- Document numeric, money, datetime

- Check Py_BuildValue("is#", status, str_buff, buff_len) for props GET.

- PyUnit

- Installation instructions

- Bulkcopy copy out
